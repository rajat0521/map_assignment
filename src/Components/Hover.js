import React from 'react'
import styled from 'styled-components'

const Hover = ( { top, left, right, down } ) => {
  return (
    <div>
      
    </div>
  )
}

const coordinate = styled.div``

export default Hover